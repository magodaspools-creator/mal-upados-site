// Camada visual do Perfil: progresso, equipamentos e detalhes de combate.
(()=>{
  const STYLE='arena-profile-advanced-style';
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const fmt=n=>new Intl.NumberFormat('pt-BR').format(Math.floor(Number(n)||0));
  const totalXp=()=>{if(typeof game==='undefined'||!game)return 0;const l=Math.max(1,Number(game.level)||1),x=Math.max(0,Number(game.xp)||0),c=l-1;return Math.floor(100*c+65*c*(c-1)/2+x)};
  const item=slot=>{const id=game?.shopEquipped?.[slot];if(id&&typeof SHOP_ITEMS!=='undefined')return SHOP_ITEMS.find(x=>x.id===id)||null;return null};
  const itemName=(slot,fallback)=>item(slot)?.name||fallback;
  const equipment=()=>[
    ['Amuleto','amulet','Nenhum'],['Helmet','helmets','Nenhum'],['Backpack','backpack','Nenhuma'],
    ['Arma','weapon',typeof WEAPONS!=='undefined'?WEAPONS[game?.weapon||0]?.[0]||'Espada de Bronze':'Espada de Bronze'],
    ['Armadura','armor',typeof ARMORS!=='undefined'?ARMORS[game?.armor||0]?.[0]||'Leather Armor':'Leather Armor'],['Escudo','shield','Nenhum'],['Ring','rings','Nenhum'],['Boots','boots','Nenhuma']
  ];
  function install(){if(document.getElementById(STYLE))return;const s=document.createElement('style');s.id=STYLE;s.textContent=`
    .arena-profile-xp{margin:0 0 14px;padding:12px 14px;border:1px solid #34383e;background:#121417}.arena-profile-xp-head{display:flex;justify-content:space-between;color:#969ca5;font-size:.6rem;text-transform:uppercase;letter-spacing:.7px}.arena-profile-xp-head strong{color:#e4c36c}.arena-profile-xp-track{height:7px;margin-top:8px;background:#292d32;overflow:hidden;border:1px solid #0b0c0e}.arena-profile-xp-track i{display:block;height:100%;background:linear-gradient(90deg,#80612d,#d7b55d)}
    .arena-profile-detail-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.arena-profile-detail{border:1px solid #30343a;background:#141619;padding:12px}.arena-profile-detail h4{margin:0 0 9px;color:#c8ccd2;font:800 .68rem Cinzel,serif;text-transform:uppercase;letter-spacing:.8px}.arena-equip-mini{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:6px}.arena-equip-mini div{min-width:0;padding:7px 5px;border:1px solid #292d32;background:#101215;text-align:center}.arena-equip-mini span{display:block;color:#727983;font-size:.48rem;text-transform:uppercase}.arena-equip-mini strong{display:block;margin-top:3px;color:#d5d8dc;font-size:.55rem;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.arena-combat-line{display:flex;justify-content:space-between;padding:6px 0;border-bottom:1px solid #292d32;color:#858b94;font-size:.62rem}.arena-combat-line:last-child{border-bottom:0}.arena-combat-line b{color:#e0e3e7}@media(max-width:650px){.arena-profile-detail-grid{grid-template-columns:1fr}.arena-equip-mini{grid-template-columns:repeat(2,minmax(0,1fr))}}
  `;document.head.appendChild(s)}
  function render(){const modal=document.getElementById('arenaAccountModal');if(!modal||typeof game==='undefined'||!game)return;const body=modal.querySelector('#arenaAccountBody');if(!body||body.dataset.profileAdvanced!=='1')return;if(modal.querySelector('.arena-profile-advanced'))return;const level=Math.max(1,Number(game.level)||1),xp=Math.max(0,Number(game.xp)||0),need=100+(level-1)*65,pct=Math.min(100,xp/need*100),eq=equipment();const d={atk:15+level*4+(Number(game.weapon)||0)*3,def:(Number(game.armor)||0)*3,hp:110+level*12+(Number(game.armor)||0)*3};const wrap=document.createElement('div');wrap.className='arena-profile-advanced';wrap.innerHTML=`<div class="arena-profile-xp"><div class="arena-profile-xp-head"><span>Progresso para Level ${fmt(level+1)}</span><strong>${fmt(xp)} / ${fmt(need)} XP</strong></div><div class="arena-profile-xp-track"><i style="width:${pct}%"></i></div></div><div class="arena-profile-detail-grid"><div class="arena-profile-detail"><h4>Equipamento atual</h4><div class="arena-equip-mini">${eq.map(x=>`<div title="${esc(itemName(x[1],x[2]))}"><span>${esc(x[0])}</span><strong>${esc(itemName(x[1],x[2]))}</strong></div>`).join('')}</div></div><div class="arena-profile-detail"><h4>Combate</h4><div class="arena-combat-line"><span>Ataque base</span><b>${fmt(d.atk)}</b></div><div class="arena-combat-line"><span>Defesa base</span><b>${fmt(d.def)}</b></div><div class="arena-combat-line"><span>Vida máxima</span><b>${fmt(d.hp)}</b></div><div class="arena-combat-line"><span>XP total</span><b>${fmt(totalXp())}</b></div><div class="arena-combat-line"><span>Dano causado</span><b>${fmt(game.damage)}</b></div></div></div>`;body.appendChild(wrap)}
  function watch(){const modal=document.getElementById('arenaAccountModal');if(!modal)return;const body=modal.querySelector('#arenaAccountBody');if(!body)return;body.dataset.profileAdvanced=modal.querySelector('#arenaAccountTitle')?.textContent==='Perfil'?'1':'0';if(body.dataset.profileAdvanced==='1')render()}
  install();setInterval(watch,500);
})();