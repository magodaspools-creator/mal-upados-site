name: Fix favicon on all pages

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  fix-favicon:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Normalize favicon on every HTML page
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re
          favicon='<link rel="icon" type="image/jpeg" href="guilda.jpg?v=3">'
          for p in Path('.').glob('*.html'):
              s=p.read_text()
              s=re.sub(r'<link[^>]+rel=["\'](?:icon|shortcut icon)["\'][^>]*>', '', s, flags=re.I)
              marker='<meta name="viewport"'
              pos=s.find('>', s.find(marker))
              if pos >= 0:
                  s=s[:pos+1]+favicon+s[pos+1:]
              p.write_text(s)
          PY
      - name: Commit if needed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add '*.html'
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Padronizar favicon em todas as páginas"
          git push
